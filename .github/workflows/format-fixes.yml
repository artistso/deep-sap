name: Format current fixes once

on:
  push:
    branches:
      - bootstrap/stabilization-v0.2
    paths:
      - '.github/workflows/format-fixes.yml'

permissions:
  contents: write

jobs:
  rustfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: bootstrap/stabilization-v0.2
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply canonical Rust formatting
        run: cargo fmt --all
      - name: Remove one-use workflow and commit
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/format-fixes.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: format compiler fixes"
          git push origin HEAD:bootstrap/stabilization-v0.2
